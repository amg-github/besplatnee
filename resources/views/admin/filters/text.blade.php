<input type="text" name="{{ $filter['name'] }}" value="{{ request()->input($filter['name'], '') }}" placeholder="{{ $filter['title'] }}" style="width: 100%;height: 25px;display: block;padding: 0px 12px;">